# Database Configuration (Supabase)
# IMPORTANT: Supabase requires two different URLs for proper operation

# Direct connection for Prisma migrations (prisma db push, prisma migrate)
# Use port 5432 without pgbouncer
DIRECT_URL="postgresql://USER:PASSWORD@HOST.supabase.co:5432/postgres"

# Pooled connection for application runtime (better for serverless)
# Use port 6543 with pgbouncer=true
DATABASE_URL="postgresql://USER:PASSWORD@HOST.supabase.co:6543/postgres?pgbouncer=true"

# Vercel Cron Job Security
# Generate a secure random secret: openssl rand -base64 32
CRON_SECRET="your-secure-random-secret-here"

# Google Places API (optional - for scraping)
# Get your API key at: https://console.cloud.google.com/apis/credentials
# Free tier: 40,000 requests/month
GOOGLE_PLACES_API_KEY="your-google-places-api-key"

# Mapbox (optional - for enhanced maps)
# Get your token at: https://account.mapbox.com/access-tokens/
MAPBOX_TOKEN="your-mapbox-token"

# SerpApi (alternative to Google Places API)
# Get your free key at: https://serpapi.com/users/sign_up
# Free tier: 100 searches/month
SERPAPI_KEY="your-serpapi-key"

# SEO & Site Configuration
# Base URL of your deployed site (used for sitemap, Open Graph, etc.)
NEXT_PUBLIC_BASE_URL="https://activityaround.vercel.app"

# Google Search Console verification code (optional)
# Get from: https://search.google.com/search-console
NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION="your-verification-code"

# Google Analytics measurement ID (optional)
# Get from: https://analytics.google.com/
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
